from django.urls import path
from . import views

urlpatterns = [
    
    # You might also have a path for your index page if you access it via a URL
    # path('index/', views.index, name='index'), 
]